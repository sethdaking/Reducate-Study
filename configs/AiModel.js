const {
    GoogleGenerativeAI,
    HarmCategory,
    HarmBlockThreshold,
  } = require("@google/generative-ai");
  
  const apiKey = process.env.NEXT_PUBLIC_GEMINI_API_KEY;
  const genAI = new GoogleGenerativeAI(apiKey);
  
  const model = genAI.getGenerativeModel({
    model: "gemini-2.0-flash",
  });
  
  const generationConfig = {
    temperature: 1,
    topP: 0.95,
    topK: 40,
    maxOutputTokens: 8192,
    responseMimeType: "application/json",
  };
  
    export const courseOutline = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Generate study material for Computer Science 1015 for exam preparation and the level of difficulty will be hard with a summary of the course. List of chapters along with a summary for each chapter, Topic list for each chapter, all results must be in JSON format, don't use '\n' for new lines use line breaks"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```json\n{\n  \"course_name\": \"Computer Science 1015: Foundations of Computation\",\n  \"course_summary\": \"This course provides a rigorous introduction to the fundamental concepts of computer science.  It covers topics ranging from basic data structures and algorithms to fundamental principles of computer architecture and operating systems.  Emphasis is placed on both theoretical understanding and practical application, with a focus on problem-solving and computational thinking. Students will learn to analyze the efficiency and correctness of algorithms and to design and implement efficient solutions to computational problems.  The course also lays the groundwork for more advanced studies in computer science.\",\n  \"chapters\": [\n    {\n      \"chapter_number\": 1,\n      \"chapter_title\": \"Introduction to Algorithms and Data Structures\",\n      \"chapter_summary\": \"This chapter introduces fundamental concepts of algorithms and data structures. It focuses on defining algorithms, analyzing their efficiency using Big O notation, and understanding the properties of basic data structures like arrays, linked lists, stacks, and queues. It also covers recursion and its applications.\",\n      \"topics\": [\n        \"Definition of an Algorithm\",\n        \"Algorithm Analysis: Time and Space Complexity\",\n        \"Big O Notation: O(1), O(log n), O(n), O(n log n), O(n^2), O(2^n), O(n!)\",\n        \"Best, Average, and Worst-Case Analysis\",\n        \"Arrays: Static vs. Dynamic Arrays, Operations and Complexity\",\n        \"Linked Lists: Singly, Doubly, Circular, Operations and Complexity\",\n        \"Stacks: LIFO, Implementation with Arrays and Linked Lists, Applications (e.g., function call stack)\",\n        \"Queues: FIFO, Implementation with Arrays and Linked Lists, Applications (e.g., scheduling)\",\n        \"Recursion: Base Case, Recursive Step, Tail Recursion, Recursion vs. Iteration, Call Stack Overflow\",\n        \"Dynamic Programming: Overlapping Subproblems, Optimal Substructure, Top-down (memoization) vs Bottom-up (tabulation) approaches (Introduction)\"\n\n      ]\n    },\n    {\n      \"chapter_number\": 2,\n      \"chapter_title\": \"Sorting and Searching Algorithms\",\n      \"chapter_summary\": \"This chapter explores various sorting and searching algorithms. It covers the principles behind each algorithm, their implementation, and their performance characteristics. It includes both comparison-based (e.g., merge sort, quicksort) and non-comparison-based (e.g., counting sort, radix sort) sorting algorithms. The chapter also examines different search algorithms, including linear search, binary search, and hashing.\",\n      \"topics\": [\n        \"Sorting Algorithms: Insertion Sort, Selection Sort, Bubble Sort, Merge Sort, Quick Sort, Heap Sort, Counting Sort, Radix Sort, Bucket Sort\",\n        \"Algorithm Analysis: Time and Space Complexity of Each Sorting Algorithm\",\n        \"Stability of Sorting Algorithms\",\n        \"Choosing the Appropriate Sorting Algorithm for a Given Scenario\",\n        \"Searching Algorithms: Linear Search, Binary Search, Interpolation Search\",\n        \"Hashing: Hash Functions, Collision Resolution (Chaining, Open Addressing - Linear Probing, Quadratic Probing, Double Hashing)\",\n        \"Hash Table Performance Analysis\",\n        \"Perfect Hashing\",\n        \"Comparison of Search Algorithm performance and complexity\"\n      ]\n    },\n    {\n      \"chapter_number\": 3,\n      \"chapter_title\": \"Trees and Graphs\",\n      \"chapter_summary\": \"This chapter covers tree and graph data structures, their properties, and algorithms for traversing and manipulating them. It discusses binary trees, binary search trees, balanced trees (e.g., AVL trees, Red-Black trees), and general graph representations (e.g., adjacency matrix, adjacency list). It also introduces graph traversal algorithms (e.g., breadth-first search, depth-first search) and shortest-path algorithms (e.g., Dijkstra's algorithm, Bellman-Ford algorithm).\",\n      \"topics\": [\n        \"Trees: Binary Trees, Binary Search Trees (BST), Balanced Trees (AVL Trees, Red-Black Trees), B-Trees\",\n        \"Tree Traversal Algorithms: Inorder, Preorder, Postorder, Level Order\",\n        \"Tree Operations: Insertion, Deletion, Search\",\n        \"Self-Balancing Trees: Rotations, Properties of AVL and Red-Black Trees\",\n        \"Graphs: Directed vs. Undirected Graphs, Weighted vs. Unweighted Graphs\",\n        \"Graph Representations: Adjacency Matrix, Adjacency List\",\n        \"Graph Traversal Algorithms: Breadth-First Search (BFS), Depth-First Search (DFS)\",\n        \"Shortest-Path Algorithms: Dijkstra's Algorithm, Bellman-Ford Algorithm, Floyd-Warshall Algorithm\",\n        \"Minimum Spanning Tree: Kruskal's Algorithm, Prim's Algorithm\",\n        \"Topological Sorting\",\n        \"Strongly Connected Components\"\n      ]\n    },\n    {\n      \"chapter_number\": 4,\n      \"chapter_title\": \"Computer Architecture Fundamentals\",\n      \"chapter_summary\": \"This chapter introduces the basic components of a computer system, including the central processing unit (CPU), memory, and input/output (I/O) devices. It covers instruction set architectures (ISAs), memory organization, caching, and pipelining. It also discusses the von Neumann architecture and its limitations.\",\n      \"topics\": [\n        \"Von Neumann Architecture\",\n        \"CPU: Components (ALU, Control Unit, Registers), Instruction Cycle (Fetch, Decode, Execute)\",\n        \"Instruction Set Architecture (ISA): Instruction Formats, Addressing Modes\",\n        \"Memory Hierarchy: Cache Memory (L1, L2, L3 Cache), Main Memory (RAM), Secondary Storage\",\n        \"Cache Mapping: Direct Mapping, Associative Mapping, Set-Associative Mapping\",\n        \"Cache Replacement Policies: LRU, FIFO, LFU\",\n        \"Pipelining: Instruction Pipelining, Hazards (Data Hazards, Control Hazards, Structural Hazards), Hazard Resolution Techniques\",\n        \"Input/Output (I/O) Devices: I/O Controllers, Direct Memory Access (DMA)\",\n        \"Interrupts: Interrupt Handling\",\n        \"Multiprocessing and Multicore Architectures\",\n        \"Amdahl's Law\"\n      ]\n    },\n    {\n      \"chapter_number\": 5,\n      \"chapter_title\": \"Operating System Concepts\",\n      \"chapter_summary\": \"This chapter covers fundamental operating system (OS) concepts, including process management, memory management, file systems, and I/O management. It discusses process scheduling algorithms, memory allocation techniques, virtual memory, file system organization, and I/O device drivers.\",\n      \"topics\": [\n        \"Operating System (OS) Structure: Kernel, System Calls\",\n        \"Process Management: Process States, Process Control Block (PCB), Process Creation and Termination\",\n        \"Process Scheduling: Scheduling Algorithms (FCFS, SJF, Priority Scheduling, Round Robin), Context Switching\",\n        \"Inter-Process Communication (IPC): Shared Memory, Message Passing\",\n        \"Threads: Multithreading, Thread Synchronization (Mutexes, Semaphores, Condition Variables)\",\n        \"Deadlock: Deadlock Prevention, Deadlock Avoidance (Banker's Algorithm), Deadlock Detection and Recovery\",\n        \"Memory Management: Virtual Memory, Paging, Segmentation\",\n        \"Page Replacement Algorithms: FIFO, LRU, Optimal\",\n        \"File Systems: File System Organization, File Allocation Methods (Contiguous Allocation, Linked Allocation, Indexed Allocation)\",\n        \"I/O Management: I/O Device Drivers, Disk Scheduling Algorithms (FCFS, SSTF, SCAN, C-SCAN)\",\n        \"Security: Access Control, Authentication, Authorization\"\n\n      ]\n    }\n  ]\n}\n```"},
          ],
        },
      ],
    });

    export const generateNotes = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Generate detailed exam notes material content for each chapter, make sure to include all topics in the content, make sure to give the content in HTML format but do not add these HTML tags(html, head, body, title). The chapters are:\n\"topics\": [\n\"Redux Advanced: Thunks, Sagas, Selectors, Redux Toolkit\",\n\"MobX: Observables, Actions, Reactions, Computed Values\",\n\"Zustand: Simpler State Management with Minimal Boilerplate\",\n\"Recoil: Fine-Grained State Management for Large Applications\",\n\"Context API: Advanced Use Cases, Performance Optimization\",\n\"Custom Hooks for State Management\",\n\"Immutability Techniques for State Updates\",\n\"Comparison of Different State Management Libraries\",\n\"Performance Considerations: Memoization and Selector Optimization\",\n\"Choosing the Right State Management Solution\"\n]"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```html\n  <h1>Redux Advanced: Thunks, Sagas, Selectors, Redux Toolkit</h1>\n\n  <h2>Thunks</h2>\n  <ul>\n    <li><strong>Definition:</strong> Middleware that allows you to write action creators that return a function instead of an action.</li>\n    <li><strong>Purpose:</strong> Handles asynchronous logic (e.g., API calls) within Redux actions.</li>\n    <li><strong>How it Works:</strong>\n      <ol>\n        <li>The thunk middleware intercepts actions that are functions.</li>\n        <li>It executes the function, passing in <code>dispatch</code> and <code>getState</code> as arguments.</li>\n        <li>The function then dispatches regular actions based on the asynchronous operation's outcome (e.g., success, failure).</li>\n      </ol>\n    </li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        // Action creator returning a thunk\n        const fetchPosts = () => {\n          return async (dispatch, getState) => {\n            dispatch({ type: 'FETCH_POSTS_REQUEST' });\n            try {\n              const response = await fetch('/api/posts');\n              const data = await response.json();\n              dispatch({ type: 'FETCH_POSTS_SUCCESS', payload: data });\n            } catch (error) {\n              dispatch({ type: 'FETCH_POSTS_FAILURE', payload: error.message });\n            }\n          };\n        };\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong> Clean separation of concerns, testable asynchronous logic.</li>\n  </ul>\n\n  <h2>Sagas</h2>\n  <ul>\n    <li><strong>Definition:</strong> Middleware that provides an alternative approach to managing side effects (asynchronous operations) in Redux.</li>\n    <li><strong>Purpose:</strong> Makes side effects easier to manage, more efficient to execute, easy to test, and better at handling failures.</li>\n    <li><strong>How it Works:</strong>\n      <ol>\n        <li>Sagas are generator functions that listen for specific actions.</li>\n        <li>When an action is dispatched, the saga is triggered.</li>\n        <li>Sagas use effects (e.g., <code>takeEvery</code>, <code>takeLatest</code>, <code>call</code>, <code>put</code>) to interact with the Redux store and perform asynchronous operations.</li>\n      </ol>\n    </li>\n    <li><strong>Key Effects:</strong>\n      <ul>\n        <li><code>takeEvery</code>: Starts a new saga on each dispatched action.</li>\n        <li><code>takeLatest</code>: Only allows one instance of the saga to run at a time (cancels previous ones).</li>\n        <li><code>call</code>: Calls a function (can be asynchronous).</li>\n        <li><code>put</code>: Dispatches an action.</li>\n        <li><code>select</code>: Retrieves data from the Redux store.</li>\n      </ul>\n    </li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        import { takeEvery, call, put } from 'redux-saga/effects';\n\n        function* fetchPostsSaga() {\n          try {\n            const response = yield call(fetch, '/api/posts');\n            const data = yield response.json();\n            yield put({ type: 'FETCH_POSTS_SUCCESS', payload: data });\n          } catch (error) {\n            yield put({ type: 'FETCH_POSTS_FAILURE', payload: error.message });\n          }\n        }\n\n        function* watchFetchPosts() {\n          yield takeEvery('FETCH_POSTS_REQUEST', fetchPostsSaga);\n        }\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong> Excellent for complex asynchronous workflows, easier to test and debug than thunks.</li>\n  </ul>\n\n  <h2>Selectors</h2>\n  <ul>\n    <li><strong>Definition:</strong> Functions that extract specific pieces of data from the Redux store.</li>\n    <li><strong>Purpose:</strong> Encapsulate the logic for accessing the store's state and deriving values.</li>\n    <li><strong>Benefits:</strong>\n      <ul>\n        <li>Improve code reusability and maintainability.</li>\n        <li>Optimize performance by preventing unnecessary re-renders (especially with memoization).</li>\n        <li>Abstract away the store's structure from components.</li>\n      </ul>\n    </li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        const getPosts = (state) => state.posts.items;\n        const getIsLoading = (state) => state.posts.isLoading;\n\n        // In a component:\n        const posts = useSelector(getPosts);\n        const isLoading = useSelector(getIsLoading);\n      </code></pre>\n    </li>\n    <li><strong>Memoization with Reselect:</strong>\n      <ul>\n        <li>Reselect is a library for creating memoized selectors.</li>\n        <li>Memoization prevents recalculation of derived data if the input state hasn't changed.</li>\n        <li>Example:\n            <pre><code>\n              import { createSelector } from 'reselect';\n\n              const getPosts = (state) => state.posts.items;\n              const getSearchTerm = (state) => state.search.term;\n\n              const getFilteredPosts = createSelector(\n                [getPosts, getSearchTerm],\n                (posts, searchTerm) => {\n                  return posts.filter(post => post.title.includes(searchTerm));\n                }\n              );\n            </code></pre>\n        </li>\n      </ul>\n    </li>\n  </ul>\n\n  <h2>Redux Toolkit</h2>\n  <ul>\n    <li><strong>Definition:</strong> Official Redux package that simplifies Redux development.</li>\n    <li><strong>Purpose:</strong> Reduces boilerplate code and provides best practices for Redux development.</li>\n    <li><strong>Key Features:</strong>\n      <ul>\n        <li><code>configureStore</code>: Simplifies store creation with pre-configured middleware (Thunk).</li>\n        <li><code>createReducer</code>: Simplifies reducer creation with Immer for immutable updates.</li>\n        <li><code>createAction</code>: Simplifies action creation.</li>\n        <li><code>createSlice</code>: Combines reducer logic and action creators into a single unit.</li>\n      </ul>\n    </li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        import { configureStore, createSlice } from '@reduxjs/toolkit';\n\n        const postsSlice = createSlice({\n          name: 'posts',\n          initialState: {\n            items: [],\n            isLoading: false,\n            error: null,\n          },\n          reducers: {\n            fetchPostsRequest(state) {\n              state.isLoading = true;\n              state.error = null;\n            },\n            fetchPostsSuccess(state, action) {\n              state.isLoading = false;\n              state.items = action.payload;\n            },\n            fetchPostsFailure(state, action) {\n              state.isLoading = false;\n              state.error = action.payload;\n            },\n          },\n        });\n\n        export const { fetchPostsRequest, fetchPostsSuccess, fetchPostsFailure } = postsSlice.actions;\n\n        export const store = configureStore({\n          reducer: {\n            posts: postsSlice.reducer,\n          },\n        });\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong> Reduces boilerplate, improves developer experience, promotes best practices.</li>\n  </ul>\n\n  <h1>MobX: Observables, Actions, Reactions, Computed Values</h1>\n\n  <h2>Observables</h2>\n  <ul>\n    <li><strong>Definition:</strong> Core concept in MobX; data that MobX tracks for changes.</li>\n    <li><strong>Purpose:</strong>  Represents the application's state. When an observable changes, MobX automatically updates any parts of the application that depend on that observable.</li>\n    <li><strong>How to Define:</strong> Use <code>observable</code> decorator or function.\n      <pre><code>\n        import { observable, makeObservable } from \"mobx\";\n\n        class Store {\n          @observable count = 0;\n\n          constructor() {\n            makeObservable(this);\n          }\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Actions</h2>\n  <ul>\n    <li><strong>Definition:</strong> Functions that modify observables.</li>\n    <li><strong>Purpose:</strong> Ensure that state mutations are tracked and trigger reactions efficiently.</li>\n    <li><strong>How to Define:</strong> Use <code>action</code> decorator or function.\n      <pre><code>\n        import { observable, action, makeObservable } from \"mobx\";\n\n        class Store {\n          @observable count = 0;\n\n          constructor() {\n            makeObservable(this);\n          }\n\n          @action\n          increment() {\n            this.count++;\n          }\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Reactions</h2>\n  <ul>\n    <li><strong>Definition:</strong> Automatically run functions when observables they depend on change.</li>\n    <li><strong>Purpose:</strong> Update the UI or perform other side effects based on state changes.</li>\n    <li><strong>Types of Reactions:</strong>\n      <ul>\n        <li><code>autorun</code>: Runs immediately and whenever any of its dependencies change.</li>\n        <li><code>reaction</code>: Similar to <code>autorun</code> but allows more control over when the reaction runs. Requires specifying data dependencies explicitly.</li>\n        <li><code>when</code>: Runs a function only once when a specific condition becomes true.</li>\n      </ul>\n    </li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        import { autorun } from \"mobx\";\n\n        const store = new Store();\n\n        autorun(() => {\n          console.log(\"Count has changed:\", store.count);\n        });\n\n        store.increment(); // Output: Count has changed: 1\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Computed Values</h2>\n  <ul>\n    <li><strong>Definition:</strong> Values that are automatically derived from observables.</li>\n    <li><strong>Purpose:</strong>  Provide a way to efficiently calculate and cache derived data.</li>\n    <li><strong>How to Define:</strong> Use <code>computed</code> decorator or function.\n      <pre><code>\n        import { observable, computed, makeObservable } from \"mobx\";\n\n        class Store {\n          @observable count = 0;\n\n          constructor() {\n            makeObservable(this);\n          }\n\n          @computed\n          get doubleCount() {\n            return this.count * 2;\n          }\n        }\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong> Automatically updated when dependencies change, cached to prevent unnecessary recalculations.</li>\n  </ul>\n\n  <h1>Zustand: Simpler State Management with Minimal Boilerplate</h1>\n\n  <h2>Core Concepts</h2>\n  <ul>\n    <li><strong>Definition:</strong> Small, fast and scalable bearbones state-management solution using simplified flux principles. Has a comfy API based on hooks.</li>\n    <li><strong>Key Features:</strong>\n        <ul>\n            <li>Minimal boilerplate:  Very little setup required.</li>\n            <li>Hooks-based API:  Uses React hooks for easy access to state.</li>\n            <li>Centralized state:  Provides a single source of truth for application state.</li>\n            <li>Simple updates:  State updates are performed directly on the state object.</li>\n            <li>Easy to learn:  Simple and intuitive API.</li>\n        </ul>\n    </li>\n  </ul>\n\n  <h2>Creating a Store</h2>\n    <ul>\n        <li>Using the <code>create</code> function.\n        </li>\n        <li>The <code>create</code> function accepts a function that defines the store's state and actions.\n        </li>\n        <li>Example:\n            <pre><code>\n                import { create } from 'zustand'\n\n                const useStore = create((set) => ({\n                  count: 0,\n                  increment: () => set((state) => ({ count: state.count + 1 })),\n                  decrement: () => set((state) => ({ count: state.count - 1 })),\n                }))\n            </code></pre>\n    </ul>\n\n  <h2>Using the Store in Components</h2>\n    <ul>\n        <li>Accessing the state and actions using the custom hook.</li>\n        <li>Example:\n            <pre><code>\n                import useStore from './store'\n\n                function Counter() {\n                  const { count, increment, decrement } = useStore()\n\n                  return (\n                    &lt;div&gt;\n                      &lt;h1&gt;{count}&lt;/h1&gt;\n                      &lt;button onClick={increment}&gt;Increment&lt;/button&gt;\n                      &lt;button onClick={decrement}&gt;Decrement&lt;/button&gt;\n                    &lt;/div&gt;\n                  )\n                }\n            </code></pre>\n    </ul>\n\n  <h2>Updating State</h2>\n    <ul>\n        <li>Using the <code>set</code> function.\n        </li>\n        <li>The <code>set</code> function accepts either a partial state object or a function that returns a partial state object.\n        </li>\n        <li>Example:\n            <pre><code>\n                const useStore = create((set) => ({\n                  count: 0,\n                  increment: () => set({ count: 1 }), // Direct State Update\n                  decrement: () => set((state) => ({ count: state.count - 1 })), // Functional State Update\n                }))\n            </code></pre>\n    </ul>\n\n  <h2>Selectors</h2>\n    <ul>\n        <li>Selecting specific parts of the state.\n        </li>\n        <li>Example:\n            <pre><code>\n                import { create } from 'zustand'\n\n                const useStore = create((set) => ({\n                  count: 0,\n                  increment: () => set((state) => ({ count: state.count + 1 })),\n                }))\n\n                const useCount = () => useStore((state) => state.count)\n\n                function CounterDisplay() {\n                  const count = useCount()\n\n                  return &lt;h1&gt;{count}&lt;/h1&gt;\n                }\n            </code></pre>\n    </ul>\n\n  <h2>Middleware</h2>\n    <ul>\n        <li>Extending Zustand with middleware (e.g., persist, immer).</li>\n        <li>Example (Persist):\n            <pre><code>\n                import { create } from 'zustand'\n                import { persist } from 'zustand/middleware'\n\n                const useStore = create(persist(\n                  (set) => ({\n                    count: 0,\n                    increment: () => set((state) => ({ count: state.count + 1 })),\n                  }),\n                  {\n                    name: 'my-store', // unique name\n                  }\n                ))\n            </code></pre>\n        </li>\n    </ul>\n\n  <h1>Recoil: Fine-Grained State Management for Large Applications</h1>\n\n  <h2>Core Concepts</h2>\n  <ul>\n    <li><strong>Atoms:</strong> Units of state that components can subscribe to.</li>\n    <li><strong>Selectors:</strong> Pure functions that derive state from atoms or other selectors.</li>\n  </ul>\n\n  <h2>Atoms</h2>\n  <ul>\n    <li><strong>Definition:</strong> Represents a piece of state in Recoil.</li>\n    <li><strong>Purpose:</strong> Components subscribe to atoms and re-render when the atom's value changes.</li>\n    <li><strong>How to Define:</strong> Use the <code>atom</code> function.\n      <pre><code>\n        import { atom } from 'recoil';\n\n        const countState = atom({\n          key: 'countState', // unique ID\n          default: 0, // initial value\n        });\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Selectors</h2>\n  <ul>\n    <li><strong>Definition:</strong> A pure function that derives state from atoms or other selectors.</li>\n    <li><strong>Purpose:</strong> Encapsulate derived state logic and memoize calculations.</li>\n    <li><strong>How to Define:</strong> Use the <code>selector</code> function.\n      <pre><code>\n        import { atom, selector } from 'recoil';\n\n        const countState = atom({\n          key: 'countState',\n          default: 0,\n        });\n\n        const doubleCountState = selector({\n          key: 'doubleCountState',\n          get: ({ get }) => {\n            const count = get(countState);\n            return count * 2;\n          },\n        });\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong> Memoization prevents unnecessary recalculations, improves performance.</li>\n  </ul>\n\n  <h2>Using Atoms and Selectors in Components</h2>\n  <ul>\n    <li>Accessing and modifying atoms using the <code>useRecoilState</code> hook.</li>\n    <li>Accessing selectors using the <code>useRecoilValue</code> hook.</li>\n    <li>Example:\n      <pre><code>\n        import { useRecoilState, useRecoilValue } from 'recoil';\n        import { countState, doubleCountState } from './atoms';\n\n        function Counter() {\n          const [count, setCount] = useRecoilState(countState);\n          const doubleCount = useRecoilValue(doubleCountState);\n\n          return (\n            &lt;div&gt;\n              &lt;h1&gt;Count: {count}&lt;/h1&gt;\n              &lt;h2&gt;Double Count: {doubleCount}&lt;/h2&gt;\n              &lt;button onClick={() => setCount(count + 1)}&gt;Increment&lt;/button&gt;\n            &lt;/div&gt;\n          );\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Asynchronous Data with Selectors</h2>\n  <ul>\n    <li>Fetching data using selectors.</li>\n    <li>Example:\n      <pre><code>\n        import { atom, selector } from 'recoil';\n\n        const postsState = atom({\n          key: 'postsState',\n          default: [],\n        });\n\n        const fetchPostsState = selector({\n          key: 'fetchPostsState',\n          get: async () => {\n            const response = await fetch('/api/posts');\n            const data = await response.json();\n            return data;\n          },\n        });\n\n        // In a component:\n        const posts = useRecoilValue(fetchPostsState);\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>RecoilRoot</h2>\n  <ul>\n    <li>The RecoilRoot component must wrap the application to provide the Recoil context.</li>\n    <li>Example:\n      <pre><code>\n        import { RecoilRoot } from 'recoil';\n\n        function App() {\n          return (\n            &lt;RecoilRoot&gt;\n              &lt;Counter /&gt;\n            &lt;/RecoilRoot&gt;\n          );\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h1>Context API: Advanced Use Cases, Performance Optimization</h1>\n\n  <h2>Advanced Use Cases</h2>\n  <ul>\n    <li><strong>Theming:</strong> Providing a theme object to all components in the application.</li>\n    <li><strong>Authentication:</strong> Sharing authentication status and user data.</li>\n    <li><strong>Locale/Internationalization:</strong> Providing the current locale to all components.</li>\n    <li><strong>Device Context:</strong>  Sharing device information (e.g., screen size, orientation) across the application.</li>\n  </ul>\n\n  <h2>Theming Example</h2>\n  <ul>\n    <li>Creating a theme context and provider.\n      <pre><code>\n        import React, { createContext, useState, useContext } from 'react';\n\n        const ThemeContext = createContext();\n\n        const ThemeProvider = ({ children }) => {\n          const [theme, setTheme] = useState('light');\n\n          const toggleTheme = () => {\n            setTheme(theme === 'light' ? 'dark' : 'light');\n          };\n\n          const value = { theme, toggleTheme };\n\n          return (\n            &lt;ThemeContext.Provider value={value}&gt;\n              {children}\n            &lt;/ThemeContext.Provider&gt;\n          );\n        };\n\n        const useTheme = () => useContext(ThemeContext);\n\n        export { ThemeProvider, useTheme };\n      </code></pre>\n    </li>\n    <li>Using the theme context in components.\n      <pre><code>\n        import React from 'react';\n        import { useTheme } from './ThemeContext';\n\n        function MyComponent() {\n          const { theme, toggleTheme } = useTheme();\n\n          return (\n            &lt;div style={{ backgroundColor: theme === 'light' ? '#fff' : '#333', color: theme === 'light' ? '#000' : '#fff' }}&gt;\n              &lt;p&gt;Current Theme: {theme}&lt;/p&gt;\n              &lt;button onClick={toggleTheme}&gt;Toggle Theme&lt;/button&gt;\n            &lt;/div&gt;\n          );\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Performance Optimization</h2>\n  <ul>\n    <li><strong>Problem:</strong>  Context can cause unnecessary re-renders of components that consume it, even if the specific values they use haven't changed.</li>\n    <li><strong>Solutions:</strong>\n      <ul>\n        <li><strong>useMemo:</strong>  Memoize values passed to the context provider.</li>\n        <li><strong>React.memo:</strong> Memoize components that consume context.</li>\n        <li><strong>Splitting Contexts:</strong>  Create multiple smaller contexts instead of one large one.</li>\n        <li><strong>Selectors within Context:</strong> Provide derived values instead of raw state.</li>\n      </ul>\n    </li>\n  </ul>\n\n  <h2>useMemo Example</h2>\n  <ul>\n    <li>Memoizing the context value using <code>useMemo</code>.\n      <pre><code>\n        import React, { createContext, useState, useContext, useMemo } from 'react';\n\n        const ThemeContext = createContext();\n\n        const ThemeProvider = ({ children }) => {\n          const [theme, setTheme] = useState('light');\n\n          const toggleTheme = () => {\n            setTheme(theme === 'light' ? 'dark' : 'light');\n          };\n\n          const value = useMemo(() => ({ theme, toggleTheme }), [theme]); // Memoize the value\n\n          return (\n            &lt;ThemeContext.Provider value={value}&gt;\n              {children}\n            &lt;/ThemeContext.Provider&gt;\n          );\n        };\n\n        const useTheme = () => useContext(ThemeContext);\n\n        export { ThemeProvider, useTheme };\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>React.memo Example</h2>\n  <ul>\n    <li>Memoizing the component using <code>React.memo</code>.\n      <pre><code>\n        import React from 'react';\n        import { useTheme } from './ThemeContext';\n\n        function MyComponent({data}) {\n          const { theme, toggleTheme } = useTheme();\n\n          return (\n            &lt;div style={{ backgroundColor: theme === 'light' ? '#fff' : '#333', color: theme === 'light' ? '#000' : '#fff' }}&gt;\n              &lt;p&gt;Current Theme: {theme}&lt;/p&gt;\n              &lt;p&gt;Data: {data}&lt;/p&gt;\n              &lt;button onClick={toggleTheme}&gt;Toggle Theme&lt;/button&gt;\n            &lt;/div&gt;\n          );\n        }\n\n        export default React.memo(MyComponent);\n      </code></pre>\n    </li>\n  </ul>\n\n  <h1>Custom Hooks for State Management</h1>\n\n  <h2>Basic Custom Hook Example</h2>\n  <ul>\n    <li>Creating a custom hook to manage a counter.\n      <pre><code>\n        import { useState } from 'react';\n\n        function useCounter(initialValue = 0) {\n          const [count, setCount] = useState(initialValue);\n\n          const increment = () => {\n            setCount(count + 1);\n          };\n\n          const decrement = () => {\n            setCount(count - 1);\n          };\n\n          const reset = () => {\n            setCount(initialValue);\n          };\n\n          return {\n            count,\n            increment,\n            decrement,\n            reset,\n          };\n        }\n\n        export default useCounter;\n      </code></pre>\n    </li>\n    <li>Using the custom hook in a component.\n      <pre><code>\n        import React from 'react';\n        import useCounter from './useCounter';\n\n        function Counter() {\n          const { count, increment, decrement, reset } = useCounter(10);\n\n          return (\n            &lt;div&gt;\n              &lt;h1&gt;Count: {count}&lt;/h1&gt;\n              &lt;button onClick={increment}&gt;Increment&lt;/button&gt;\n              &lt;button onClick={decrement}&gt;Decrement&lt;/button&gt;\n              &lt;button onClick={reset}&gt;Reset&lt;/button&gt;\n            &lt;/div&gt;\n          );\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Managing API Data with Custom Hooks</h2>\n  <ul>\n    <li>Creating a custom hook to fetch data from an API.\n      <pre><code>\n        import { useState, useEffect } from 'react';\n\n        function useFetch(url) {\n          const [data, setData] = useState(null);\n          const [loading, setLoading] = useState(true);\n          const [error, setError] = useState(null);\n\n          useEffect(() => {\n            const fetchData = async () => {\n              try {\n                const response = await fetch(url);\n                if (!response.ok) {\n                  throw new Error(`HTTP error! Status: ${response.status}`);\n                }\n                const json = await response.json();\n                setData(json);\n                setLoading(false);\n              } catch (error) {\n                setError(error);\n                setLoading(false);\n              }\n            };\n\n            fetchData();\n          }, [url]);\n\n          return { data, loading, error };\n        }\n\n        export default useFetch;\n      </code></pre>\n    </li>\n    <li>Using the custom hook in a component.\n      <pre><code>\n        import React from 'react';\n        import useFetch from './useFetch';\n\n        function Posts() {\n          const { data: posts, loading, error } = useFetch('/api/posts');\n\n          if (loading) return &lt;p&gt;Loading...&lt;/p&gt;;\n          if (error) return &lt;p&gt;Error: {error.message}&lt;/p&gt;;\n\n          return (\n            &lt;ul&gt;\n              {posts.map(post => (\n                &lt;li key={post.id}&gt;{post.title}&lt;/li&gt;\n              ))}\n            &lt;/ul&gt;\n          );\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Sharing State Between Components</h2>\n  <ul>\n    <li>Using custom hooks with Context API to share state.\n      <pre><code>\n        import React, { createContext, useContext, useState } from 'react';\n\n        const AuthContext = createContext();\n\n        function useAuth() {\n          return useContext(AuthContext);\n        }\n\n        function AuthProvider({ children }) {\n          const [user, setUser] = useState(null);\n\n          const login = (userData) => {\n            setUser(userData);\n          };\n\n          const logout = () => {\n            setUser(null);\n          };\n\n          const value = {\n            user,\n            login,\n            logout,\n          };\n\n          return (\n            &lt;AuthContext.Provider value={value}&gt;\n              {children}\n            &lt;/AuthContext.Provider&gt;\n          );\n        }\n\n        export { AuthProvider, useAuth };\n      </code></pre>\n    </li>\n    <li>Consuming the authentication context in components.\n      <pre><code>\n        import React from 'react';\n        import { useAuth } from './AuthProvider';\n\n        function Profile() {\n          const { user, logout } = useAuth();\n\n          if (!user) return &lt;p&gt;Please login.&lt;/p&gt;;\n\n          return (\n            &lt;div&gt;\n              &lt;h1&gt;Welcome, {user.name}&lt;/h1&gt;\n              &lt;button onClick={logout}&gt;Logout&lt;/button&gt;\n            &lt;/div&gt;\n          );\n        }\n      </code></pre>\n    </li>\n  </ul>\n\n  <h1>Immutability Techniques for State Updates</h1>\n\n  <h2>Why Immutability?</h2>\n  <ul>\n    <li><strong>Predictability:</strong> Makes it easier to reason about state changes.</li>\n    <li><strong>Change Detection:</strong> Enables efficient change detection in React (e.g., <code>shouldComponentUpdate</code>, <code>React.memo</code>).</li>\n    <li><strong>Debugging:</strong>  Simplifies debugging by providing a clear history of state changes.</li>\n    <li><strong>Time Travel Debugging:</strong> Allows for time travel debugging in tools like Redux DevTools.</li>\n  </ul>\n\n  <h2>Techniques for Immutability</h2>\n  <ul>\n    <li><strong>Copying Objects:</strong> Creating new objects instead of modifying existing ones.</li>\n    <li><strong>Copying Arrays:</strong> Creating new arrays instead of mutating existing ones.</li>\n    <li><strong>Using Immutable Data Structures:</strong> Using libraries like Immutable.js or Immer.</li>\n  </ul>\n\n  <h2>Copying Objects</h2>\n  <ul>\n    <li><strong>Object.assign():</strong>\n      <pre><code>\n        const originalObject = { name: 'John', age: 30 };\n        const newObject = Object.assign({}, originalObject, { age: 31 });\n\n        console.log(originalObject); // { name: 'John', age: 30 }\n        console.log(newObject);      // { name: 'John', age: 31 }\n      </code></pre>\n    </li>\n    <li><strong>Spread Operator:</strong>\n      <pre><code>\n        const originalObject = { name: 'John', age: 30 };\n        const newObject = { ...originalObject, age: 31 };\n\n        console.log(originalObject); // { name: 'John', age: 30 }\n        console.log(newObject);      // { name: 'John', age: 31 }\n      </code></pre>\n    </li>\n    <li><strong>Deep Copying:</strong> For nested objects, use libraries like Lodash's <code>cloneDeep</code> or implement a custom deep copy function.\n      <pre><code>\n        import cloneDeep from 'lodash/cloneDeep';\n\n        const originalObject = { name: 'John', address: { city: 'New York' } };\n        const newObject = cloneDeep(originalObject);\n\n        newObject.address.city = 'Los Angeles';\n\n        console.log(originalObject.address.city); // New York\n        console.log(newObject.address.city);      // Los Angeles\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Copying Arrays</h2>\n  <ul>\n    <li><strong>slice():</strong>\n      <pre><code>\n        const originalArray = [1, 2, 3];\n        const newArray = originalArray.slice(); // Creates a shallow copy\n\n        newArray.push(4);\n\n        console.log(originalArray); // [1, 2, 3]\n        console.log(newArray);      // [1, 2, 3, 4]\n      </code></pre>\n    </li>\n    <li><strong>Spread Operator:</strong>\n      <pre><code>\n        const originalArray = [1, 2, 3];\n        const newArray = [...originalArray, 4];\n\n        console.log(originalArray); // [1, 2, 3]\n        console.log(newArray);      // [1, 2, 3, 4]\n      </code></pre>\n    </li>\n    <li><strong>map():</strong>  For transforming arrays.\n      <pre><code>\n        const originalArray = [1, 2, 3];\n        const newArray = originalArray.map(x => x * 2);\n\n        console.log(originalArray); // [1, 2, 3]\n        console.log(newArray);      // [2, 4, 6]\n      </code></pre>\n    </li>\n    <li><strong>filter():</strong> For filtering arrays.\n      <pre><code>\n        const originalArray = [1, 2, 3, 4, 5];\n        const newArray = originalArray.filter(x => x % 2 === 0);\n\n        console.log(originalArray); // [1, 2, 3, 4, 5]\n        console.log(newArray);      // [2, 4]\n      </code></pre>\n    </li>\n  </ul>\n\n  <h2>Using Immutable.js</h2>\n  <ul>\n    <li><strong>Introduction:</strong> Provides immutable data structures like Lists, Maps, and Sets.</li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        import { Map } from 'immutable';\n\n        const originalMap = Map({ name: 'John', age: 30 });\n        const newMap = originalMap.set('age', 31);\n\n        console.log(originalMap.get('age')); // 30\n        console.log(newMap.get('age'));      // 31\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong>  Guaranteed immutability, efficient data structures.</li>\n  </ul>\n\n  <h2>Using Immer</h2>\n  <ul>\n    <li><strong>Introduction:</strong> Simplifies immutable updates by allowing you to work with mutable drafts.</li>\n    <li><strong>Example:</strong>\n      <pre><code>\n        import produce from 'immer';\n\n        const originalObject = { name: 'John', age: 30 };\n\n        const newObject = produce(originalObject, draft => {\n          draft.age = 31;\n        });\n\n        console.log(originalObject); // { name: 'John', age: 30 }\n        console.log(newObject);      // { name: 'John', age: 31 }\n      </code></pre>\n    </li>\n    <li><strong>Benefits:</strong> Easier to use than Immutable.js, performs well.</li>\n  </ul>\n\n  <h1>Comparison of Different State Management Libraries</h1>\n\n  <h2>Redux</h2>\n  <ul>\n    <li><strong>Pros:</strong>\n      <ul>\n        <li>Mature and widely adopted.</li>\n        <li>Large ecosystem of middleware and tools.</li>\n        <li>Predictable state management.</li>\n        <li>Excellent for complex applications with many interactions.</li>\n      </ul>\n    </li>\n    <li><strong>Cons:</strong>\n      <ul>\n        <li>Significant boilerplate.</li>\n        <li>Steep learning curve.</li>\n        <li>Can be overkill for simple applications.</li>\n      </ul>\n    </li>\n    <li><strong>Use Cases:</strong> Complex applications with many data interactions, applications requiring time-travel debugging.</li>\n  </ul>\n\n  <h2>MobX</h2>\n  <ul>\n    <li><strong>Pros:</strong>\n      <ul>\n        <li>Less boilerplate than Redux.</li>\n        <li>Easy to learn and use.</li>\n        <li>Automatic dependency tracking.</li>\n        <li>Good for applications with complex UI interactions.</li>\n      </ul>\n    </li>\n    <li><strong>Cons:</strong>\n      <ul>\n        <li>Can be less predictable than Redux.</li>\n        <li>Smaller ecosystem compared to Redux.</li>\n        <li>Potentially harder to debug in very large applications due to implicit reactivity.</li>\n      </ul>\n    </li>\n    <li><strong>Use Cases:</strong> Applications with complex UI interactions, rapid prototyping.</li>\n  </ul>\n\n  <h2>Zustand</h2>\n  <ul>\n    <li><strong>Pros:</strong>\n      <ul>\n        <li>Minimal boilerplate.</li>\n        <li>Very easy to learn and use.</li>\n        <li>Simple and intuitive API.</li>\n        <li>Good performance.</li>\n      </ul>\n    </li>\n    <li><strong>Cons:</strong>\n      <ul>\n        <li>Smaller ecosystem compared to Redux and MobX.</li>\n        <li>Less suitable for very complex applications with intricate data flows.</li>\n      </ul>\n    </li>\n    <li><strong>Use Cases:</strong> Small to medium-sized applications, simple state management needs, projects where minimizing boilerplate is a priority.</li>\n  </ul>\n\n  <h2>Recoil</h2>\n  <ul>\n    <li><strong>Pros:</strong>\n      <ul>\n        <li>Fine-grained state management.</li>\n        <li>Efficient re-renders.</li>\n        <li>Good for large applications with complex state requirements.</li>\n        <li>Easy to use with React's concurrent mode.</li>\n      </ul>\n    </li>\n    <li><strong>Cons:</strong>\n      <ul>\n        <li>Relatively new compared to Redux and MobX.</li>\n        <li>Smaller ecosystem.</li>\n        <li>Can be more complex than Zustand for simple use cases.</li>\n      </ul>\n    </li>\n    <li><strong>Use Cases:</strong> Large applications with complex state requirements, applications where performance is critical.</li>\n  </ul>\n\n  <h2>Context API</h2>\n  <ul>\n    <li><strong>Pros:</strong>\n      <ul>\n        <li>Built"},
          ],
        },
      ],
    });

    export const generateStudyType = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Create detailed flashcards with a tricky way of asking questions on Next.js 15 and SSR, caching and other topics of equal difficulty in JSON format with front content(Question) and back content(answer)\n"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```json\n[\n  {\n    \"front\": \"Question: In Next.js 15, if a component is marked with `cache: 'force-cache'`, does it *guarantee* that the data will *never* be refetched during the lifetime of the application? Explain why or why not.\",\n    \"back\": \"Answer: No, it doesn't guarantee *never*. While `cache: 'force-cache'` aims for indefinite caching, it's *not* immutable. Here's why:\\n\\n1. **Deployment Changes:** A new deployment *will* clear the cache.\\n2. **Manual Cache Invalidation:**  The Next.js runtime (or a third-party cache store) might have mechanisms to invalidate the cache, even for `force-cache`. This could be due to memory pressure or explicit cache clearing APIs.\\n3. **Server Actions with `revalidatePath` or `revalidateTag`:** If a server action associated with this component uses `revalidatePath` or `revalidateTag` and is triggered, the cached data may be invalidated even if the component is `force-cache` marked.\\n4. **Upgrades/Updates:** Upgrades to Next.js or related libraries can occasionally cause cache invalidation.\\n\\nSo, `force-cache` should be understood as a strong *hint* for indefinite caching, but not a guaranteed contract. Always design your application assuming that data *can* be re-fetched.\"\n  },\n  {\n    \"front\": \"Question: Consider a Next.js 15 route that uses Server-Side Rendering (SSR) with `getServerSideProps`. How can you prevent a user from seeing the 'flash of content' that often occurs before the page fully renders on the client-side?\",\n    \"back\": \"Answer: The primary method is to leverage **`useSWR` or `useQuery` (or similar data fetching libraries) *with initial data provided from `getServerSideProps`***. This eliminates the need for the component to initially render with a loading state.\\n\\nHere's why this works:\\n\\n1. **`getServerSideProps` fetches data server-side:**  `getServerSideProps` allows you to fetch the data *before* the component is rendered on either the server or the client.\\n2. **Pass data to `useSWR`/`useQuery`:**  Pass the data fetched in `getServerSideProps` as the `fallbackData` option (for `useSWR`) or `initialData` option (for `useQuery`).\\n3. **Client-side hydration with data:** `useSWR` or `useQuery` will hydrate the component on the client with this initial data, effectively bypassing the loading state and the flash of content.\\n\\n**Example (using `useSWR`):**\\n\\n```javascript\\nimport useSWR from 'swr';\\n\\nfunction MyPage({ initialData }) {\\n  const { data, error } = useSWR('/api/data', fetcher, { fallbackData: initialData });\\n\\n  if (error) return <div>Failed to load</div>\\n  return <div>{data.message}</div>\\n}\\n\\nexport async function getServerSideProps() {\\n  const res = await fetch('http://localhost:3000/api/data');\\n  const initialData = await res.json();\\n  return { props: { initialData } };\\n}\\n\\nexport default MyPage;\\n```\\n\\nThis ensures that the client-side component renders immediately with the data fetched server-side, eliminating the flash of content.\"\n  },\n  {\n    \"front\": \"Question: Explain the differences between `useRouter().push()` and `useRouter().replace()` in Next.js 15. Specifically, how do they affect the browser's history stack?\",\n    \"back\": \"Answer:\\n\\n*   **`useRouter().push()`**: Adds a new entry to the browser's history stack. This means the user can use the browser's 'Back' button to navigate to the previous page.\\n\\n*   **`useRouter().replace()`**: Replaces the current entry in the browser's history stack with the new one.  The user *cannot* use the 'Back' button to go back to the page from which `replace()` was called.  The previous page is effectively removed from the history.\\n\\n**Analogy:**\\n\\n*   `push()` is like adding a new page to a book. You can flip back to the previous pages.\\n*   `replace()` is like tearing out the current page and replacing it with a new one. The original page is gone.\\n\\n**Use Cases:**\\n\\n*   Use `push()` for typical navigation where you want the user to be able to go back (e.g., navigating between products, articles, etc.).\\n*   Use `replace()` for:\\n    *   Redirects after form submissions (to prevent resubmission on 'Back' press).\\n    *   Authentication flows (after login, you likely don't want the user to be able to go back to the login page).\\n    *   Cleanly updating the URL without adding unnecessary history entries (e.g., filtering options on a search page).  However, consider using `useSearchParams` in Next.js 13+ for search params as a better pattern.\\n\\n**In summary:** `push()` adds to the history, `replace()` overwrites the history.\"\n  },\n  {\n    \"front\": \"Question: You are building a Next.js 15 application that needs to display frequently updated stock prices.  You want the server to pre-render the initial page (for SEO and faster first load), but also want to update the prices regularly on the client-side.  What is the *best* approach to achieve this, and what are its benefits?\",\n    \"back\": \"Answer: The best approach is to combine **Server-Side Rendering (SSR) with client-side data fetching using `useSWR` or `useQuery` with a polling interval**.\\n\\n**Steps:**\\n\\n1.  **SSR for Initial Render:** Use `getServerSideProps` to fetch the initial stock prices on the server and pass them as props to your component.\\n2.  **`useSWR`/`useQuery` with Polling:** In your React component, use `useSWR` or `useQuery` to fetch the latest stock prices on the client-side. Configure a polling interval (e.g., every 5 seconds) to automatically refetch the data.\\n\\n**Example (using `useSWR`):**\\n\\n```javascript\\nimport useSWR from 'swr';\\n\\nfunction StockPrice({ initialPrice }) {\\n  const { data, error } = useSWR('/api/stock-price', fetcher, { refreshInterval: 5000, fallbackData: { price: initialPrice }});\\n\\n  if (error) return <div>Error loading price</div>;\\n  if (!data) return <div>Loading...</div>; // Should only appear if initial render fails\\n\\n  return <div>Stock Price: {data.price}</div>;\\n}\\n\\nexport async function getServerSideProps() {\\n  const res = await fetch('http://localhost:3000/api/stock-price');\\n  const initialPriceData = await res.json();\\n  const initialPrice = initialPriceData.price; // Extract initial price from data\\n\\n  return { props: { initialPrice } };\\n}\\n\\nexport default StockPrice;\\n```\\n\\n**Benefits:**\\n\\n*   **SEO and Faster First Load:** SSR provides the initial HTML content with the stock price, making it crawlable by search engines and giving users a faster initial page load.\\n*   **Real-time Updates:** Client-side polling ensures that the displayed stock prices are regularly updated without requiring a full page reload.\\n*   **Good User Experience:** The initial content is immediately visible, and subsequent updates are seamlessly applied in the background.\\n*   **Reduced Server Load:** Instead of constantly SSR rendering on every request, the server only handles the initial render.  Client-side updates reduce the frequency of server requests.\\n\\n**Why this is better than other options:**\\n\\n*   **Static Site Generation (SSG):** Not suitable for frequently changing data.\\n*   **Client-Side Rendering (CSR) alone:** Poor SEO and slower initial load.\\n*   **SSR on every request:**  High server load and potential performance issues.\"\n  },\n  {\n    \"front\": \"Question: Explain how to implement optimistic updates in a Next.js 15 application using Server Actions. Provide a code example demonstrating a scenario where a user likes a post, and the UI updates immediately before the server confirms the like.\",\n    \"back\": \"Answer: Optimistic updates improve the perceived performance of an application by immediately updating the UI in response to a user action, *before* the server confirms the success of the operation. If the server operation fails, the UI is reverted to its previous state.\\n\\n**Steps:**\\n\\n1. **Define a Server Action:** Create a server action to handle the like operation.\\n2. **Local State Update:**  Before calling the server action, update the UI immediately (optimistically) to reflect the liked state.\\n3. **Call the Server Action:**  Call the server action to persist the like on the server.\\n4. **Handle Success/Error:** If the server action succeeds, do nothing (the optimistic update is correct). If the server action fails, revert the UI to its previous state.\\n\\n**Code Example:**\\n\\n```javascript\n    'use client';\n\n    import { useState } from 'react';\n    import { likePost } from './actions'; // Assume this file contains the server action\n\n    async function LikeButton({ postId, initialLikes }) {\n      const [likes, setLikes] = useState(initialLikes);\n      const [isLiked, setIsLiked] = useState(false);\n      const [isLiking, setIsLiking] = useState(false);\n\n      const handleLike = async () => {\n        setIsLiking(true);\n        setIsLiked(true);\n        setLikes(prevLikes => prevLikes + 1); // Optimistic Update\n\n        try {\n          await likePost(postId); // Call the server action\n        } catch (error) {\n          console.error('Error liking post:', error);\n          setIsLiked(false);\n          setLikes(prevLikes => prevLikes - 1); // Revert on Error\n        } finally {\n          setIsLiking(false);\n        }\n      };\n\n      return (\n        <button onClick={handleLike} disabled={isLiking}>\n          {isLiked ? 'Unlike' : 'Like'} ({likes})\n        </button>\n      );\n    }\n\n    export default LikeButton;\n    ```\n\n    ```javascript\n    // actions.js (Server Action - needs to be in a separate file)\n    'use server';\n\n    export async function likePost(postId) {\n      // Simulate a database update\n      await new Promise(resolve => setTimeout(resolve, 500)); // Simulate latency\n\n      // Simulate an error sometimes\n      if (Math.random() < 0.2) {\n        throw new Error('Failed to like post');\n      }\n\n      console.log(`Post ${postId} liked!`);\n      return { success: true };\n    }\n    ```\n\n**Explanation:**\n\n1.  **`LikeButton` Component:**  The client component manages the local state for the number of likes and whether the post is liked.\n\n2.  **`handleLike` Function:**  This function is triggered when the user clicks the like button.\n    *   It first optimistically updates the UI by incrementing the `likes` count and setting `isLiked` to `true`.\n    *   Then, it calls the `likePost` server action.\n    *   If the server action fails (e.g., due to a network error or database issue), it catches the error and reverts the UI to its previous state by decrementing the `likes` count and setting `isLiked` to `false`.\n\n3. **`likePost` Server Action:** The server action simulates an asynchronous database update (using `setTimeout`) and introduces a chance of failure (for demonstration purposes).  In a real application, this would perform the actual database update.\n\n**Key Considerations:**\n\n*   **Error Handling:** Robust error handling is crucial.  Provide feedback to the user if the optimistic update fails.\n*   **Idempotency:** Server actions should ideally be idempotent (safe to execute multiple times) to handle potential retries.\n*   **Data Consistency:** Ensure that the optimistic update logic aligns with the server-side logic to minimize discrepancies.\n*   **Alternative Loading States:** Show an intermediate state (`isLiking`) while waiting for the server response to prevent multiple clicks.\"\n  },\n  {\n    \"front\": \"Question: Explain how you would implement rate limiting in a Next.js 15 API route.  Discuss different strategies for storing and managing the rate limits, considering scalability and performance.\",\n    \"back\": \"Answer: Rate limiting protects your API endpoints from abuse by limiting the number of requests a client can make within a specified time window.\\n\\n**Implementation Strategies:**\\n\\n1.  **In-Memory Storage (Simple, Non-Scalable):**\\n    *   Use a JavaScript object or Map to store request counts per IP address or client identifier.\\n    *   Use a timer (e.g., `setTimeout` or `setInterval`) to reset the counters after the time window expires.\\n    *   **Pros:** Simple to implement, no external dependencies.\\n    *   **Cons:** Not scalable for multiple server instances (each instance has its own memory), not persistent (resets on server restart), difficult to coordinate rate limits across servers.\\n\\n    **Example:**\\n\\n    ```javascript\n    // pages/api/rate-limited.js\n    let requestCounts = {}; // In-memory storage\n\n    export default async function handler(req, res) {\n      const ip = req.headers['x-forwarded-for'] || req.socket.remoteAddress;\n      const now = Date.now();\n      const windowMs = 60 * 1000; // 1 minute window\n      const maxRequests = 10;\n\n      if (!requestCounts[ip]) {\n        requestCounts[ip] = [];\n      }\n\n      requestCounts[ip] = requestCounts[ip].filter(time => time > now - windowMs);\n\n      if (requestCounts[ip].length >= maxRequests) {\n        return res.status(429).send('Too Many Requests');\n      }\n\n      requestCounts[ip].push(now);\n      // your API logic here\n      res.status(200).json({ message: 'API Request Successful' });\n    }\n    ```\n\n2.  **Redis (Scalable, Persistent):**\\n    *   Use Redis (an in-memory data store) to store request counts and timestamps.\\n    *   Redis offers atomic operations (e.g., incrementing a counter) which are crucial for concurrency.\\n    *   Use Redis's `EXPIRE` command to automatically expire rate limit data after the time window.\\n    *   **Pros:** Scalable for multiple server instances, persistent (data survives server restarts), good performance.\\n    *   **Cons:** Requires setting up and managing a Redis server, adds a dependency.\\n\\n    **Example (using `ioredis`):**\\n\\n    ```javascript\n    // pages/api/rate-limited.js\n    import Redis from 'ioredis';\n\n    const redis = new Redis(process.env.REDIS_URL); // Configure Redis connection\n\n    export default async function handler(req, res) {\n      const ip = req.headers['x-forwarded-for'] || req.socket.remoteAddress;\n      const windowSeconds = 60; // 1 minute window\n      const maxRequests = 10;\n      const key = `rate_limit:${ip}`;\n\n      try {\n        // Use Redis's INCR and EXPIRE commands\n        const current = await redis.incr(key);\n\n        if (current === 1) {\n          // Set expiration time only on the first request in the window\n          await redis.expire(key, windowSeconds);\n        }\n\n        if (current > maxRequests) {\n          return res.status(429).send('Too Many Requests');\n        }\n\n        // your API logic here\n        res.status(200).json({ message: 'API Request Successful' });\n\n      } catch (error) {\n        console.error('Redis error:', error);\n        return res.status(500).send('Internal Server Error');\n      }\n    }\n    ```\n\n3. **Dedicated Rate Limiting Services (Most Scalable, Feature-Rich):**\n    *   Use a dedicated rate-limiting service like Cloudflare Rate Limiting, AWS WAF Rate-Based Rules, or Kong API Gateway.\n    *   These services handle rate limiting at the edge of your network, before requests even reach your Next.js server.\n    *   **Pros:** Highly scalable, handles complex rate-limiting scenarios, integrates with other security features.\n    *   **Cons:**  Higher cost, increased complexity in setup and configuration, dependent on a third-party service.\\n\\n**Choosing the Right Strategy:**\\n\\n*   **Simple API, low traffic:** In-memory storage might be sufficient for initial development and low-traffic APIs.\\n*   **Scalable API, moderate traffic:** Redis is a good balance between scalability, performance, and ease of use.\\n*   **High-traffic API, complex requirements:** A dedicated rate-limiting service is the best option for production environments requiring high scalability, advanced features, and robust security.\\n\\n**Additional Considerations:**\\n\\n*   **Client Identification:**  Use IP address, API key, or user ID to identify clients.\\n*   **Time Window:** Choose an appropriate time window (e.g., seconds, minutes, hours) based on your API's usage patterns.\\n*   **HTTP Status Code:** Return a `429 Too Many Requests` status code when the rate limit is exceeded, along with a `Retry-After` header indicating how long the client should wait before retrying.\\n*   **Error Handling:** Implement robust error handling to gracefully handle Redis connection errors or other issues.\\n*   **Monitoring:** Monitor your rate limits to identify potential abuse or unexpected traffic patterns.\"\n  },\n  {\n    \"front\": \"Question: Describe the concept of 'Partial Hydration' in the context of Next.js 15 and React Server Components (RSCs). Why is it important, and how does it differ from traditional client-side hydration?\",\n    \"back\": \"Answer: **Partial Hydration:**\\n\\nPartial hydration, in the context of Next.js 15 and RSCs, is the technique of selectively hydrating only the interactive parts of a React component on the client-side.  It's a more granular and efficient approach to hydration compared to traditional methods.\\n\\n**Traditional Client-Side Hydration (with Next.js Pages Router):**\\n\\nIn traditional client-side hydration, the entire React component tree is initially rendered on the server as HTML. When the page loads in the browser, React 'hydrates' the entire tree, attaching event listeners and making it interactive. This can be inefficient, especially if a significant portion of the UI is static (e.g., text, images, read-only content).\\n\\n**How Partial Hydration Works with RSCs:**\\n\\nRSCs are rendered entirely on the server.  Only the *interactive* parts of the UI need to be hydrated on the client. Next.js (or React itself with upcoming features) can identify these interactive parts and hydrate only those components.  The rest of the UI remains as static HTML.\\n\\n**Key Differences:**\\n\\n| Feature           | Traditional Client-Side Hydration | Partial Hydration (with RSCs) |\\n| ----------------- | --------------------------------- | ------------------------------ |\\n| Hydration Scope   | Entire Component Tree             | Selective, Interactive Parts   |\\n| JavaScript Bundle | Larger (hydrates everything)      | Smaller (hydrates less)      |\\n| Performance       | Slower initial load, more work for the client | Faster initial load, less client-side work |\\n\\n**Why is Partial Hydration Important?**\\n\\n*   **Improved Performance:** By hydrating only the interactive parts, the client-side JavaScript bundle size is significantly reduced. This leads to faster initial page load times and improved Time to Interactive (TTI).\\n*   **Better User Experience:** Faster load times contribute to a smoother and more responsive user experience.\\n*   **Reduced Client-Side Work:** The browser has less work to do during hydration, freeing up resources for other tasks.\\n*   **Enhanced SEO:**  Faster initial render from SSR and reduced JavaScript execution improve SEO.\\n\\n**Challenges:**\\n\\n*   **Determining Hydration Boundaries:** Accurately identifying which components need hydration can be complex. React's `use client` directive helps developers define client-side components.\\n*   **State Management:** Coordinating state between server components and client components requires careful planning.\\n*   **Library Compatibility:** Not all existing React libraries may be fully compatible with RSCs and partial hydration.  Libraries need to be updated to support server components.\\n\\n**In summary:** Partial hydration is a key performance optimization enabled by React Server Components. It reduces client-side JavaScript and improves the overall user experience by selectively hydrating only the interactive elements of a web application.\"\n  }\n]\n```"},
          ],
        },
      ],
    });

    const chatSession = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Generate a Quiz on this topic: Computer Science 1015 advanced topics in JSON format, along with the correct answer with 4 topics, make it as abstract and hard as you can.\n"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```json\n{\n  \"quizTitle\": \"Computer Science 1015: Advanced JSON Concepts\",\n  \"description\": \"Test your knowledge of advanced topics related to JSON, including parsing complexities, schema validation, serialization nuances, and security considerations.  Assume a professional context and a need for optimized performance and robust error handling.\",\n  \"questions\": [\n    {\n      \"questionId\": 1,\n      \"questionText\": \"Consider a highly nested JSON structure with circular references that exceeds standard parser memory limits. Which strategy offers the most robust and memory-efficient approach to extract a specific deeply nested value, *without* loading the entire structure into memory?\",\n      \"options\": [\n        \"A. Using a standard DOM-based parser with increased memory allocation and recursion depth.\",\n        \"B. Employing a SAX-based parser with custom event handlers to track the path and extract the target value as the parser iterates.\",\n        \"C. Utilizing JSONPath expressions with a streaming parser that only buffers relevant sections of the document.\",\n        \"D. Transforming the JSON into a relational database and querying it using SQL.\"\n      ],\n      \"correctAnswer\": \"C\",\n      \"explanation\": \"JSONPath with a streaming parser is the most memory-efficient.  DOM-based parsers load the entire structure. SAX parsing requires complex state management for deeply nested structures. Database transformation adds significant overhead and complexity for simple extraction.\"\n    },\n    {\n      \"questionId\": 2,\n      \"questionText\": \"You're designing a REST API that returns complex JSON responses containing sensitive financial data. You need to ensure data integrity and prevent unauthorized modification during transit.  Which combination of techniques offers the strongest defense against both accidental corruption and malicious tampering of the JSON data?\",\n      \"options\": [\n        \"A. JSON Schema validation at the client and server, coupled with HTTPS for secure transport.\",\n        \"B. Using a digital signature generated with a private key and included as a separate field in the JSON response, and validating the signature with the corresponding public key upon receipt, also enforcing HTTPS.\",\n        \"C. Applying a lossless compression algorithm (e.g., gzip) to the JSON response before transmission, and decompressing it upon receipt.\",\n        \"D. Encoding the JSON response using Base64 encoding, followed by encryption with AES using a shared secret key.\"\n      ],\n      \"correctAnswer\": \"B\",\n      \"explanation\": \"Digital signatures provide cryptographic proof of origin and integrity. HTTPS ensures confidentiality. JSON schema validates structure, not integrity. Compression only reduces size. Base64 encoding provides no security; AES alone doesn't guarantee integrity against modification if the key is compromised or the IV is predictable without integrity protection.\"\n    },\n    {\n      \"questionId\": 3,\n      \"questionText\": \"In a microservices architecture, services communicate via JSON messages. Service A sends a JSON message containing a polymorphic object represented by a type discriminator field (e.g., 'type': 'Car' or 'type': 'Truck'). Service B needs to deserialize this message correctly into the appropriate object type. What is the most *flexible and maintainable* approach for handling this polymorphism in the deserialization process at Service B?\",\n      \"options\": [\n        \"A. Using a large 'if-else' or 'switch' statement to check the 'type' field and manually create the appropriate object instance.\",\n        \"B. Employing a reflection-based deserialization library that automatically maps the 'type' field to the corresponding class using a pre-configured mapping.\",\n        \"C. Utilizing a custom deserializer that registers class-specific factories based on the 'type' field, allowing for easy addition of new types without modifying the core deserialization logic.\",\n        \"D. Restructuring the JSON to avoid polymorphism altogether by using separate endpoints for each object type.\"\n      ],\n      \"correctAnswer\": \"C\",\n      \"explanation\": \"Custom deserializers with class-specific factories provide the most flexible and maintainable solution. 'If-else' statements are brittle. Reflection can be inefficient and introduce security risks. Restructuring the API is a significant change and may not always be feasible. Class-specific factories allow adding new types without touching the core deserialization logic, promoting loose coupling and maintainability.\"\n    },\n    {\n      \"questionId\": 4,\n      \"questionText\": \"You are tasked with designing a system to efficiently store and retrieve JSON documents containing temporal data, specifically sensor readings tagged with timestamps. The data volume is massive (terabytes), and queries frequently involve range-based searches on the timestamps.  Which storage solution and indexing strategy is most appropriate for optimizing query performance?\",\n      \"options\": [\n        \"A. Storing JSON documents in a relational database (e.g., PostgreSQL) with a B-tree index on the timestamp column.\",\n        \"B. Utilizing a NoSQL document database (e.g., MongoDB) with a geospatial index on the sensor location and a separate index on the timestamp.\",\n        \"C. Storing JSON documents in a time-series database (e.g., InfluxDB) optimized for temporal data with built-in indexing capabilities for timestamps and sensor metadata.\",\n        \"D. Storing JSON documents as flat files in a distributed file system (e.g., Hadoop HDFS) and using Apache Spark to process queries in parallel.\"\n      ],\n      \"correctAnswer\": \"C\",\n      \"explanation\": \"Time-series databases are specifically designed for storing and querying temporal data. They have built-in optimizations and indexing strategies tailored for time-based searches, making them significantly more efficient than general-purpose relational or NoSQL databases or flat files. Geospatial indexing is irrelevant unless location-based queries are frequent in *addition* to time-based queries.\"\n    }\n  ]\n}\n```"},
          ],
        },
      ],
    });
  
    export const GenerateQuiz = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Generate a Quiz on this topic: Computer Science 1015 advanced topics in JSON format, along with the correct answer with 4 topics, make it as abstract and hard as you can.\n"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```json\n{\n  \"quizTitle\": \"Computer Science 1015: Advanced JSON Concepts\",\n  \"description\": \"Test your knowledge of advanced topics related to JSON, including parsing complexities, schema validation, serialization nuances, and security considerations.  Assume a professional context and a need for optimized performance and robust error handling.\",\n  \"questions\": [\n    {\n      \"questionId\": 1,\n      \"questionText\": \"Consider a highly nested JSON structure with circular references that exceeds standard parser memory limits. Which strategy offers the most robust and memory-efficient approach to extract a specific deeply nested value, *without* loading the entire structure into memory?\",\n      \"options\": [\n        \"A. Using a standard DOM-based parser with increased memory allocation and recursion depth.\",\n        \"B. Employing a SAX-based parser with custom event handlers to track the path and extract the target value as the parser iterates.\",\n        \"C. Utilizing JSONPath expressions with a streaming parser that only buffers relevant sections of the document.\",\n        \"D. Transforming the JSON into a relational database and querying it using SQL.\"\n      ],\n      \"correctAnswer\": \"C\",\n      \"explanation\": \"JSONPath with a streaming parser is the most memory-efficient.  DOM-based parsers load the entire structure. SAX parsing requires complex state management for deeply nested structures. Database transformation adds significant overhead and complexity for simple extraction.\"\n    },\n    {\n      \"questionId\": 2,\n      \"questionText\": \"You're designing a REST API that returns complex JSON responses containing sensitive financial data. You need to ensure data integrity and prevent unauthorized modification during transit.  Which combination of techniques offers the strongest defense against both accidental corruption and malicious tampering of the JSON data?\",\n      \"options\": [\n        \"A. JSON Schema validation at the client and server, coupled with HTTPS for secure transport.\",\n        \"B. Using a digital signature generated with a private key and included as a separate field in the JSON response, and validating the signature with the corresponding public key upon receipt, also enforcing HTTPS.\",\n        \"C. Applying a lossless compression algorithm (e.g., gzip) to the JSON response before transmission, and decompressing it upon receipt.\",\n        \"D. Encoding the JSON response using Base64 encoding, followed by encryption with AES using a shared secret key.\"\n      ],\n      \"correctAnswer\": \"B\",\n      \"explanation\": \"Digital signatures provide cryptographic proof of origin and integrity. HTTPS ensures confidentiality. JSON schema validates structure, not integrity. Compression only reduces size. Base64 encoding provides no security; AES alone doesn't guarantee integrity against modification if the key is compromised or the IV is predictable without integrity protection.\"\n    },\n    {\n      \"questionId\": 3,\n      \"questionText\": \"In a microservices architecture, services communicate via JSON messages. Service A sends a JSON message containing a polymorphic object represented by a type discriminator field (e.g., 'type': 'Car' or 'type': 'Truck'). Service B needs to deserialize this message correctly into the appropriate object type. What is the most *flexible and maintainable* approach for handling this polymorphism in the deserialization process at Service B?\",\n      \"options\": [\n        \"A. Using a large 'if-else' or 'switch' statement to check the 'type' field and manually create the appropriate object instance.\",\n        \"B. Employing a reflection-based deserialization library that automatically maps the 'type' field to the corresponding class using a pre-configured mapping.\",\n        \"C. Utilizing a custom deserializer that registers class-specific factories based on the 'type' field, allowing for easy addition of new types without modifying the core deserialization logic.\",\n        \"D. Restructuring the JSON to avoid polymorphism altogether by using separate endpoints for each object type.\"\n      ],\n      \"correctAnswer\": \"C\",\n      \"explanation\": \"Custom deserializers with class-specific factories provide the most flexible and maintainable solution. 'If-else' statements are brittle. Reflection can be inefficient and introduce security risks. Restructuring the API is a significant change and may not always be feasible. Class-specific factories allow adding new types without touching the core deserialization logic, promoting loose coupling and maintainability.\"\n    },\n    {\n      \"questionId\": 4,\n      \"questionText\": \"You are tasked with designing a system to efficiently store and retrieve JSON documents containing temporal data, specifically sensor readings tagged with timestamps. The data volume is massive (terabytes), and queries frequently involve range-based searches on the timestamps.  Which storage solution and indexing strategy is most appropriate for optimizing query performance?\",\n      \"options\": [\n        \"A. Storing JSON documents in a relational database (e.g., PostgreSQL) with a B-tree index on the timestamp column.\",\n        \"B. Utilizing a NoSQL document database (e.g., MongoDB) with a geospatial index on the sensor location and a separate index on the timestamp.\",\n        \"C. Storing JSON documents in a time-series database (e.g., InfluxDB) optimized for temporal data with built-in indexing capabilities for timestamps and sensor metadata.\",\n        \"D. Storing JSON documents as flat files in a distributed file system (e.g., Hadoop HDFS) and using Apache Spark to process queries in parallel.\"\n      ],\n      \"correctAnswer\": \"C\",\n      \"explanation\": \"Time-series databases are specifically designed for storing and querying temporal data. They have built-in optimizations and indexing strategies tailored for time-based searches, making them significantly more efficient than general-purpose relational or NoSQL databases or flat files. Geospatial indexing is irrelevant unless location-based queries are frequent in *addition* to time-based queries.\"\n    }\n  ]\n}\n```"},
          ],
        },
      ],
    });

    // configs/AiModel.js
// Add this to your existing AiModel.js file

// configs/AiModel.js
// Add this to your existing AiModel.js file

export const generateQNA = model.startChat({
  generationConfig,
  history: [
    {
      role: "user",
      parts: [
        {text: "Generate a comprehensive set of question and answer pairs for exam preparation. The questions should test deep understanding and application of concepts, with detailed answers that explain the reasoning. Return in JSON format with this structure: { \"qna\": [ { \"question\": \"Detailed question text?\", \"answer\": \"Comprehensive answer with explanation\" } ] }"},
      ],
    },
    {
      role: "model",
      parts: [
        {text: "```json\n{\n  \"qna\": [\n    {\n      \"question\": \"Explain how Newton's Third Law applies to rocket propulsion and why rockets can accelerate in the vacuum of space despite having nothing to 'push against'.\",\n      \"answer\": \"Newton's Third Law states that for every action, there is an equal and opposite reaction. In rocket propulsion, the rocket expels high-speed exhaust gases in one direction (the action), which creates a thrust force in the opposite direction (the reaction). The rocket isn't 'pushing against' the atmosphere or any external medium; rather, it's pushing against the exhaust gases it expels. The momentum of the system is conserved: as the exhaust gases gain momentum in one direction, the rocket gains an equal amount of momentum in the opposite direction. This is why rockets can accelerate efficiently in the vacuum of space—they carry their own reaction mass (propellant) and don't need an external medium to push against.\"\n    },\n    {\n      \"question\": \"Compare and contrast the concepts of work and energy in physics. How are they related, and in what ways are they different?\",\n      \"answer\": \"Work and energy are closely related concepts in physics, but they represent different aspects of physical interactions. Energy is a state property—a quantity that a system possesses, while work is a process—something that happens during an interaction. Energy exists in various forms (kinetic, potential, thermal, etc.) and can be stored, while work is energy in transit or energy being transferred. The relationship between them is defined by the work-energy theorem, which states that the net work done on an object equals its change in kinetic energy. Mathematically, work is calculated as the dot product of force and displacement (W = F·d·cosθ), while energy is measured in the same units but represents the capacity to do work. A key difference is that energy is a scalar quantity (having only magnitude), while work involves both force (a vector) and displacement (also a vector), though the result is scalar. Additionally, work can be positive, negative, or zero depending on the angle between force and displacement, while energy is always positive or zero.\"\n    },\n    {\n      \"question\": \"Describe the quantum mechanical model of the atom and explain how it differs from the Bohr model. What experimental evidence supports the quantum mechanical model?\",\n      \"answer\": \"The quantum mechanical model of the atom, developed primarily by Schrödinger, Heisenberg, and Born in the 1920s, describes electrons as wave-like entities existing in probability clouds called orbitals, rather than in definite paths. Unlike the Bohr model, which depicts electrons in fixed circular orbits with defined radii and energies, the quantum model states that we can only know the probability of finding an electron in a certain region around the nucleus. The quantum model is characterized by four quantum numbers (principal, angular momentum, magnetic, and spin) that completely describe an electron's quantum state, and it incorporates the Heisenberg Uncertainty Principle, which states that we cannot simultaneously know an electron's exact position and momentum. Experimental evidence supporting the quantum model includes: (1) Electron diffraction experiments showing the wave-like properties of electrons, (2) Spectroscopic data revealing fine structure in atomic spectra that the Bohr model couldn't explain, (3) The Zeeman effect, where spectral lines split in a magnetic field, demonstrating the existence of electron spin, (4) Scanning tunneling microscopy images showing electron density distributions that match calculated orbital shapes, and (5) The success of the Schrödinger equation in predicting energy levels for the hydrogen atom and more complex atoms.\"\n    }\n  ]\n}\n```"},
      ],
    },
  ],
});
